# Boolavas product application URLs (public frontend URLs, configurable per environment)
# Leave blank until the production application URL is available.
# When a value is set, the corresponding "Open" CTA appears on the product page.

VITE_MIGRASAFE_APP_URL=
VITE_CONFIGSAFE_APP_URL=
